What is the time complexity of the following code:

static int func(int n) {
    int s = 0;
    for (int i = 0; i < n; i = i * 2) {
        s += i;
    }
    return s;
}

Answer: O(Infinity) as 'i' always stays 0, and the exit condition is never met